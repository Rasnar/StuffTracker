<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <!-- "Mark Favorite", should appear as action button if possible -->
    <item
        android:id="@+id/action_delete_item"
        android:icon="@drawable/ic_delete_white_48dp"
        android:title="@string/delete_item_from_database"
        app:showAsAction="always"/>
    <item
        android:id="@+id/action_validate_edit_menu"
        android:icon="@drawable/ic_done_white_48dp"
        android:title="@string/validate_item_insertion"
        app:showAsAction="always"/>
</menu>